@model List<XtremeMobiles.Models.Product>
@{
    Layout = "~/Views/Shared/_Layout2.cshtml";
}
<table class="table table-bordered" style="margin-top:20px">
    <thead>
        <tr>
            <th>Model</th>
            <th>Price</th>
            <th>Brand</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var s in Model)
        {
            <tr>
                <td>@s.Model</td>
                <td>@s.Price</td>
                <td>@s.pId</td>
            </tr>
        }
    </tbody>

</table>


